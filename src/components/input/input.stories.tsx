import {Input, InputProps} from './input';

export default {
    title: "components/Input",
    component: Input
};

export const BasicInput = (props: InputProps) => <Input style={{marginTop: '10rem'}}>Names</Input>
;


import React from 'react';
import { expect } from 'chai';
import Formlogin from './FormLogin';

describe('Spec Formlogin', function () {
    it('it exists', () => {
        expect(Formlogin).to.be.ok;
    });
});
